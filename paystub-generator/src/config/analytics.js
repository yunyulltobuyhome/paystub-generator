// Google Analytics 4.
//
// Paste your GA4 Measurement ID below to turn on analytics. Until it's set,
// no analytics script loads (the site stays clean and dependency-free).
// Create a property at analytics.google.com → Admin → Data Streams.
export const GA_MEASUREMENT_ID = '' // e.g. 'G-XXXXXXXXXX'
